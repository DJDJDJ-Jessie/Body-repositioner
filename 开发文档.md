# 身体复位提醒器开发文档

更新时间：2026-08-01

## 一句话目标

做一个像番茄钟一样的桌面身体复位工具：用户手动开始专注倒计时，倒计时结束后强制进入沉浸式复位窗口，播放本地视频；视频播放完成前不能退出，播放完成后才能点击完成回到工作。

## 当前结论

旧 WinForms 版本已经清理，不再作为后续维护对象。当前界面发糊、文字裁切、按钮像旧系统控件，根因不是单纯“画得不够好”，而是 WinForms/GDI+、固定像素布局和高 DPI 渲染天然不适合做 2026 年审美标准下的高清沉浸式界面。

后续主线改为现代桌面应用方案：

- 主线：Electron + Vue 3 + Vite + Tailwind CSS + TypeScript。
- 备选：Tauri + Vue 3 + Vite + Tailwind CSS + TypeScript。
- 选择原则：Electron 体积更大，但自带 Chromium，跨电脑兼容性更稳；Tauri 更轻，但在部分电脑上可能依赖 WebView2 环境。

当前实际开发决定：

- 已切换为 Electron + Vue 3 + Vite + Tailwind CSS + TypeScript。
- 原因：当前本机没有 Rust/Cargo，Tauri 无法直接打包；同时用户强调“拷到别人电脑直接使用”，Electron 更符合当前交付目标。
- 新版源码目录：`body-reset-electron`。
- 当前便携 EXE：`便携版/身体复位提醒器.exe`。
- 后续除非明确要求改回 Tauri，否则继续沿 Electron 便携路线开发。
- 源码目录只保留可维护文件；`node_modules`、`dist`、`release` 等依赖缓存和打包产物不长期保留，需要时重新生成。
- 最终交付优先使用免安装文件夹版，而不是单文件 portable exe。单文件 portable 会自解压，双击启动可能慢；免安装文件夹版复制整个 `便携版` 文件夹即可，启动体验更好。

## 用户不可接受的事情

- 不让用户写代码。
- 不让用户手动编辑配置文件。
- 不做旧式系统控件风格。
- 不在强制复位视频上放进度条，复位过程要沉浸。
- 不让每次都播放同一个视频。
- 不把“安装开发环境”当成交付方式；最终要给可直接使用的 Windows 程序。
- 不在复位弹窗时保留 Windows 任务栏；复位窗口要覆盖任务栏，形成完整强制感。
- 不让复位完成后停在空闲状态；正常完成后自动开始下一轮专注。

## 核心功能

1. 主界面
   - 显示当前专注时长。
   - 用户手动点击开始。
   - 支持暂停、继续、重置。
   - 支持立即复位。
   - 支持打开视频文件夹。
   - 支持可视化设置，不要求用户改配置文件。

2. 倒计时
   - 默认专注时长：50 分钟。
   - 用户可在界面里调整时长。
   - 未开始时保持安静，不自动弹窗。
   - 暂停后不继续消耗时间。

3. 强制复位窗口
   - 倒计时结束后自动出现。
   - 尽量全屏、置顶、沉浸。
   - 播放本地视频。
   - 播放时支持方向键 `↑` / `↓` 调节视频音量。
   - 播放时支持暂停/继续，不影响完成锁定。
   - 视频播放完成前，“完成”按钮不可用。
   - 视频播放完成后，“完成”按钮可点击，关闭窗口并回到工作。
   - 复位窗口覆盖整块屏幕，包括 Windows 任务栏。
   - 没有视频时，清楚提示用户把视频放入指定文件夹。

4. 视频管理
   - 用户只需要把视频文件放进 `videos` 文件夹，或在里面建立按星期使用的子文件夹。
   - 支持常见格式：mp4、mov、wmv、avi、mkv、m4v。
   - 每次复位播放不同视频。
   - 所有视频轮完后再进入下一轮。
   - 尽量避免连续两次播放同一个视频。
   - 视频播放顺序由程序自动管理，不需要用户配置。
   - 支持自动识别 `周一到周五`、`周末`、`周一`、`周二`、`周三`、`周四`、`周五`、`周六`、`周日` 等文件夹。
   - 选择优先级：当天专属文件夹 > 工作日/周末文件夹 > `videos` 根目录。

5. 可视化设置
   - 专注时长。
   - 视频文件夹位置。
   - 是否开机启动。
   - 是否最小化到托盘。
   - 是否开启严格模式。
   - 紧急退出规则。
   - 睡眠提醒开关、开始时间、结束时间和提醒间隔。
   - 睡眠提醒视频文件夹。
   - 普通复位视频音量和睡眠提醒视频音量；音量按钮与 `↑` / `↓` 调整后立即进入持久化设置。

   所有界面可修改的参数都必须写入 `data/settings.json`，程序启动时读取并恢复上一次设置；新增参数必须同时补充默认值、校验、类型定义、预览 mock 和使用说明。

6. 睡眠严格提醒
   - 在设定的睡眠时段内，只要电脑未进入睡眠且程序仍在运行，按提醒间隔触发全屏提醒。
   - 使用独立的 `sleep-reminders` 文件夹选择提醒视频，不与普通复位视频混用。
   - 睡眠提醒视频循环播放，只允许暂停/继续，不提供正常返回工作按钮。
   - 睡眠提醒同样支持方向键 `↑` / `↓`、音量减/加按钮和暂停/继续按钮。
   - 长按 `Esc` 按紧急退出规则退出；退出后固定 5 分钟再次检查并提醒。
   - 睡眠提醒窗口不受普通复位的 `strictMode` 开关影响，始终严格。

7. 今日节奏统计
   - 展示今日工作总时长。
   - 展示今日运动休息次数。
   - 展示今日复位总时长。
   - 统计数据自动保存在 `data/daily-stats.json`。
   - 用户不需要写配置或手动维护统计文件。

8. 身体账本数据表
   - 主界面展示本周、本月两种数据表。
   - 按天展示工作时长、复位次数、复位总时长和完成率。
   - 周/月汇总展示工作总时长、复位总时长、完成次数和完成率。
   - 统计数据自动保存在 `data/daily-stats.json`。
   - 同步生成 `data/daily-stats.csv`，方便后续做周/月复盘、积分、奖励和惩罚机制。

## UI 方向

主界面要像一个现代专注工具，不像传统系统弹窗。视觉关键词：

- 清晰、高清、无锯齿。
- 大面积留白或柔和背景，不做拥挤控制面板。
- 主计时器是第一视觉中心。
- 按钮状态清楚，但不要像旧 Windows 灰按钮。
- 图标使用现代线性图标。
- 字体使用系统现代字体栈，避免像素感。
- 所有文字都不能裁切、重叠、发虚。
- 不用纯色大绿底统治全屏，避免单一色调。

强制复位窗口方向：

- 视频本身是主角。
- 不显示进度条。
- 不显示播放器控制条。
- 只保留必要状态文案和完成按钮。
- 完成按钮在视频结束前禁用，结束后明确可点。
- 整体要有“现在先复位身体”的仪式感。

## 技术方案

### 当前实现

- 桌面壳：Electron。
- 界面：Vue 3。
- 构建：Vite。
- 样式：Tailwind CSS + 少量自定义 CSS。
- 语言：TypeScript。
- 图标：lucide 图标库。
- 本地能力：由 Electron 主进程负责文件扫描、按星期选择视频文件夹、睡眠提醒调度、系统睡眠/唤醒监听、状态保存、开机启动、置顶全屏窗口、托盘图标和 Windows 媒体暂停键。

### 为什么不用继续 WinForms

- 高 DPI 下容易出现模糊和缩放问题。
- 自绘控件成本高，细节仍难达到现代 Web UI 水平。
- 视频、动画、响应式布局、高清字体渲染都不如 Web 技术自然。
- 继续修会越来越像“修补旧界面”，不是做一个真正舒服的产品。

### Tauri 与 Electron 的取舍

Tauri：

- 优点：安装包小，运行轻，适合工具类应用。
- 风险：Windows 上依赖 WebView2。Windows 10 1803 及之后版本通常已有 WebView2，但非常旧或精简系统可能需要安装运行时。

Electron：

- 优点：自带 Chromium，放到别人电脑上兼容性更稳。
- 风险：体积明显更大。

本项目已经切换到 Electron。除非用户明确要求减小体积并接受 WebView2 依赖风险，否则不切回 Tauri。

打包交付时优先使用 `electron-builder --dir` 生成免安装文件夹版。单文件 portable exe 仍可生成，但不作为默认交付，因为启动时自解压会拖慢体验。

## 数据与文件

建议最终目录结构：

```text
身体复位提醒器/
  身体复位提醒器.exe
  videos/
    用户放普通复位视频到这里
    周一到周五/
    周末/
  sleep-reminders/
    用户放睡眠提醒视频到这里
  data/
    settings.json
    video-state.json
    external-log-sync.json
    reset-log.csv
    daily-stats.json
    daily-stats.csv
```

规则：

- 用户只接触 `身体复位提醒器.exe`、`videos` 和 `sleep-reminders` 文件夹。
- `data` 由程序自己维护。
- 设置文件可以存在，但必须有界面修改入口。
- 迁移旧版本时，尽量保留原有 `videos` 文件夹。
- 按星期文件夹不需要配置文件，程序按文件夹名称自动识别。
- `daily-stats.csv` 是自动生成的数据表，不要求用户维护，可用于后续奖励/惩罚规则。
- `reset-log.csv` 是每次复位的内部原始日志，所有用户都默认拥有。
- 外部日志同步默认关闭；打开后，程序把 `reset-log.csv`、`daily-stats.csv` 和 `review-sync-manifest.json` 复制到用户选择的外部文件夹。
- 外部同步只是镜像，不替代内部日志；外部路径不可用时不能阻断复位或统计写入。

设置持久化字段包括：`focusMinutes`、`resetMinutes`、`resetVolume`、`sleepReminderVolume`、睡眠提醒开关/时段/间隔、视频文件夹、严格模式、托盘运行、开机启动、紧急退出时长、`externalLogSyncEnabled` 和 `externalLogSyncFolder`。读取旧版 `settings.json` 时，缺失字段使用默认值并自动补回文件。主界面销毁时不再用未完成加载的默认状态回写设置；设置保存请求按顺序处理，并通过单实例锁避免多个程序实例互相覆盖。

## 打包交付

最终交付目标：

- Windows 可运行版本。
- 用户复制整个 `便携版` 文件夹后，双击 `身体复位提醒器.exe` 即可使用。
- 同级 `videos` 文件夹不存在时自动创建。
- 同级 `sleep-reminders` 文件夹不存在时自动创建。
- 程序第一次启动时给出清楚提示：把视频放入 `videos` 文件夹。
- 不要求用户安装 Node、Rust、.NET SDK 或写任何命令。

交付前必须验证：

- 双击启动正常。
- 没有视频时提示正常。
- 放入多个视频后能轮流播放。
- 新建 `周一到周五` 或 `周末` 文件夹后，当天只使用对应文件夹里的视频。
- 新建当天专属文件夹后，优先使用当天专属文件夹。
- 倒计时结束后能强制弹出复位窗口。
- 复位窗口覆盖 Windows 任务栏。
- 视频未结束前不能点完成。
- 视频结束后可以点完成。
- 暂停、继续、重置可用。
- 开机启动后自动开始倒计时。
- 复位完成后自动开始下一轮倒计时。
- 电脑进入睡眠时倒计时自动暂停，唤醒后自动继续。
- 睡眠时段内提醒开关打开且有提醒视频时，按间隔触发睡眠严格提醒。
- 睡眠严格提醒循环播放视频，不能正常关闭，只能暂停/继续或长按 `Esc` 紧急退出。
- 睡眠严格提醒紧急退出后 5 分钟再次提醒。
- 普通复位窗口和睡眠严格提醒窗口使用不同的关闭规则。
- 任务栏托盘有清晰图标。
- 主窗口可拖拽移动。
- 今日节奏统计能随工作和复位更新。
- 身体账本能查看本周、本月数据表。
- `data/daily-stats.csv` 能随统计更新自动生成。
- 默认关闭外部日志同步时，程序仍能完整写入内部 `data/reset-log.csv` 和 `data/daily-stats.csv`。
- 选择外部日志文件夹并开启同步后，外部文件夹能生成 `reset-log.csv`、`daily-stats.csv` 和 `review-sync-manifest.json`。
- 外部日志文件夹不可用时，程序仍能完成复位并继续写入内部日志。
- 将外部日志文件夹设置为与内部 `data` 文件夹相同的位置时，程序应提示并拒绝该设置。
- 关闭并重新打开程序后，专注/复位分钟数、睡眠提醒时段与间隔、开关、紧急退出时长和两种视频音量仍保持上次设置。
- UI 在 100%、125%、150% 缩放下不糊、不裁字。

## 开发流程

1. 在 `body-reset-electron` 中继续维护新版源码。
2. UI 改动后先本地预览并截图检查。
3. 功能改动后验证倒计时、暂停、立即复位、视频轮播和完成锁定。
4. 需要重新交付时再安装依赖并打包。
5. 用免安装文件夹版输出更新 `便携版`，确认 `videos` 和 `data` 与 exe 同级。
6. 便携测试通过后，再把 `node_modules`、`dist`、`release` 等中间产物清理掉。

## UI 验收标准

每次改 UI 后都要截图检查：

- 主界面截图。
- 设置界面截图。
- 强制复位窗口截图。
- 125% 或 150% Windows 缩放截图。

不合格情况：

- 字发糊。
- 字被裁切。
- 按钮文字挤压。
- 颜色单调像旧软件。
- 控件像 2000 年代系统默认按钮。
- 视频页出现进度条或播放器控制条。
- 视频页出现固定的紧急退出提示文案。
- 睡眠严格提醒出现普通“返回工作”按钮。

## 已安装的设计辅助 Skill

以下 skill 已安装到本机，后续 UI 开发时优先使用：

- `frontend-design`
- `web-design-guidelines`
- `design-taste-frontend`
- `shadcn-ui`

说明：安装工具提示 PromptScript 目标不支持全局安装，但 Codex 目标已成功安装。新 skill 通常会在下一轮对话中正式出现在可用列表。

`shadcn-ui` 已安装，但暂不作为主约束，因为它偏 React 组件库，而当前推荐路线是 Vue + Tailwind。后续可以参考它的组件审美与交互密度；若改为 React，再把它升级为主约束。

## 参考来源

- Stretchly：开源休息提醒应用，使用 Electron，并提供 Windows portable 打包目标。https://github.com/hovancik/stretchly/blob/trunk/package.json
- Pomatez：开源番茄钟应用，仓库中包含 Tauri 构建脚本和 Tauri CLI。https://raw.githubusercontent.com/zidoro/pomatez/master/package.json
- Super Productivity：开源任务/时间管理应用，使用 Electron 桌面入口和 Angular 前端构建。https://raw.githubusercontent.com/johannesjo/super-productivity/master/package.json
- Tauri Windows 依赖说明：Windows 开发需要 C++ Build Tools 和 WebView2，Windows 10 1803 及之后版本通常已有 WebView2。https://tauri.app/start/prerequisites/#windows
- Electron 官方说明：Electron 使用 JavaScript、HTML、CSS 构建桌面应用，并在程序中嵌入 Chromium 和 Node.js。https://www.electronjs.org/docs/latest/
