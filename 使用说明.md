# 身体复位提醒器

这是新版免安装桌面版。复制整个 `便携版` 文件夹到别人的电脑，双击里面的 `身体复位提醒器.exe` 就能使用。

## 便携版位置

当前可用程序在：

```text
便携版/身体复位提醒器.exe
```

`便携版` 里会保留：

```text
身体复位提醒器.exe
videos/
sleep-reminders/
data/
```

用户只需要把复位视频放进 `videos` 文件夹，或者在里面建立按星期使用的子文件夹。`data` 是程序自己保存设置、播放记录和统计数据的位置，不需要手动编辑。

## 设置自动记忆

程序会自动记录上一次使用的设置。下次打开后，会恢复专注时长、复位等待时长、睡眠提醒开关、睡眠提醒开始/结束时间、首次提醒间隔、严格模式、紧急退出时长、开机启动/托盘设置、睡眠唤醒后自动启动设置，以及普通复位和睡眠提醒视频的音量。设置保存在 `data/settings.json`，不需要手动编辑。

## 使用流程

1. 双击 `身体复位提醒器.exe`。
2. 把复位视频放进同级 `videos` 文件夹。
3. 回到主窗口，确认专注时长。
4. 默认情况下，程序在电脑处于解锁状态时会自动开始专注倒计时；也可以在设置里关闭“解锁后自动开始”，再手动点击“开始专注”。
5. 中途可以点击“暂停”或“重置”。手动暂停超过 5 分钟且电脑仍未锁屏时，倒计时会自动恢复。
6. 倒计时结束后，会弹出复位窗口并播放一个视频。
7. 普通复位和睡眠提醒视频都可以按方向键 `↑` / `↓` 调节音量，也可以使用画面上的音量按钮。
8. 复位视频画面提供暂停/继续播放按钮，暂停时仍可稍后继续完成复位。
9. 默认需要等视频播放完成后才能点击完成；如果打开“允许提前结束普通复位”，达到设置的最短复位时长后即可提前返回。
10. 点击“完成，回到工作”后，程序会自动开始下一轮专注倒计时。

## 睡眠联动

- 电脑进入睡眠时，正在运行的倒计时会自动暂停。
- 电脑从睡眠唤醒后，程序会检查倒计时状态；原本正在运行的倒计时会自动继续；如果当前没有运行倒计时，则由“睡眠唤醒后自动启动”控制是否开启新的倒计时。“解锁后自动开始”仍只负责程序运行期间的解锁/非锁屏规则。
- 睡眠提醒全屏出现时，主界面的专注倒计时会暂停；按 `Esc` 退出提醒后仍保持暂停，需要手动点击“继续”才会恢复。
- 倒计时结束进入复位时，程序会尝试暂停当前电脑上的浏览器或播放器媒体播放。

## 睡眠提醒

在设置里打开“睡眠提醒”，可以设定开始时间、结束时间和首次提醒间隔。设定时段内，只要电脑没有进入睡眠，程序会全屏循环播放睡眠提醒视频。睡眠提醒属于严格模式：只能暂停/继续视频，不能正常返回工作；暂停时仍然可以用方向键或音量按钮调节音量。

如果确实需要暂时离开，可以长按 `Esc` 紧急退出。首次提醒间隔最多为 5 分钟；第一次提醒退出后，后续每 1 分钟再次全屏提醒，直到电脑离开睡眠时段或停止使用。如果电脑仍在使用且仍处于睡眠时段，程序会持续催促离开工作状态。

提醒视频放在程序同级的 `sleep-reminders` 文件夹中。点击设置里的“打开睡眠提醒视频”即可打开这个文件夹。

## 视频异常等待

“视频异常等待”只在视频打不开、播放失败等异常情况使用。比如设置为 5 分钟，意思是视频无法播放时等待 5 分钟后可以返回工作。

如果视频在播放过程中发生媒体错误，程序会停止并卸载异常视频源，只进入一次异常等待，不会反复从头播放或反复重置等待时间。

正常视频不受这个设置影响，仍然必须播放完成后才能点击完成。

## 自动倒计时与提前复位

“解锁后自动开始”用于避免忘记启动倒计时：程序运行期间，只要电脑处于非锁屏状态且当前没有正在进行的复位，程序会开始专注倒计时。手动暂停后会等待 5 分钟；如果电脑在这段时间内仍未锁屏，倒计时会自动恢复。电脑锁屏时倒计时暂停，解锁后恢复。

“睡眠唤醒后自动启动”与“开机启动”是两套不同机制。打开后，程序会在 Windows 任务计划程序中登记一个“系统从低功耗状态恢复”的事件任务；电脑从睡眠恢复时，即使程序之前已经退出，Windows 也会重新启动程序并检查倒计时。它不是依靠已经退出的 Electron 进程接收 `powerMonitor` 事件。关闭这个开关后，程序会移除对应的 Windows 事件任务。

“允许提前结束普通复位”只影响普通复位视频，不影响睡眠提醒。打开后，在“最短复位时长”中设置例如 3 分钟，普通复位开始满 3 分钟后即可点击“提前完成，回到工作”；如果视频已经先播放完，则仍可立即完成。睡眠提醒始终保持严格模式。

## 今日节奏

主界面会显示今天的工作与运动休息节奏：

- 今日工作总时长
- 运动休息次数
- 复位总时长

这些数据由程序自动记录，不需要写配置。

## 身体账本

主界面会显示“身体账本”，可以切换：

- 本周数据表
- 本月数据表

数据表按天记录：

- 工作时长
- 复位次数
- 复位总时长
- 完成率

程序还会在 `data/daily-stats.csv` 自动生成一张日统计表，后续做每周/月奖励、惩罚或积分规则时，可以直接使用这份数据。

## 日志保存与外部同步

程序有两层日志保存方式。

第一层是每个用户自己的内部日志，始终自动写入程序旁边的 `data` 文件夹：

```text
data/reset-log.csv
data/daily-stats.csv
```

其中：

- `reset-log.csv` 记录每次复位的时间、完成或紧急退出结果、播放的视频名称。
- `daily-stats.csv` 记录每天的工作分钟、复位分钟、复位次数、完成次数、紧急退出次数和完成率。

第二层是可选的“外部日志同步”。它适合个人复盘、私有备份或其他数据分析，但不是程序运行的必要条件。

开启方法：

1. 打开右上角“设置”。
2. 打开“启用外部同步”。
3. 点击“选择外部同步文件夹”，选择一个你自己的文件夹。
4. 如需立即复制一次，点击“立即同步”。

外部文件夹会得到：

```text
reset-log.csv
daily-stats.csv
review-sync-manifest.json
```

程序会先写内部日志，再尝试同步外部文件夹。外部文件夹不可用时，程序仍然正常运行，内部日志不会丢失。

外部同步默认关闭。把程序复制给其他人时，不要把你自己的 `data/settings.json`、`reset-log.csv` 或外部同步路径一起复制给对方；对方仍然可以使用程序自己的内部日志。

时间说明：`reset-log.csv` 的 `time` 是 UTC 的 ISO 时间；`daily-stats.csv` 的日期按电脑本地日期生成。做每日复盘时，需要按本机时区解释逐次日志。

## 视频规则

支持这些格式：

- `.mp4`
- `.mov`
- `.wmv`
- `.avi`
- `.mkv`
- `.m4v`

如果有多个视频，程序会自动轮流播放，尽量避免连续两次播放同一个视频。

空文件会被自动忽略。如果某个视频大小是 0 字节，需要重新下载或重新复制。

可以在 `videos` 里建立按星期使用的子文件夹：

```text
videos/
  周一到周五/
  周末/
```

也可以更细：

```text
videos/
  周一/
  周二/
  周三/
  周四/
  周五/
  周六/
  周日/
```

选择顺序是：当天专属文件夹优先，其次是“周一到周五”或“周末”，都没有时使用 `videos` 根目录里的视频。

## 可视化设置

点击右上角设置按钮，可以调整：

- 专注时长
- 视频异常等待
- 视频文件夹
- 睡眠提醒开关、开始时间、结束时间和首次提醒间隔
- 睡眠提醒视频文件夹
- 严格模式
- 普通复位提前返回开关和最短复位时长
- 最小化到托盘
- 开机启动
- 解锁后自动开始
- 紧急退出秒数

开启“开机启动”后，程序随系统启动时会自动开始倒计时。

## 安全出口

普通复位窗口和睡眠提醒窗口都会尽量全屏置顶，并覆盖 Windows 任务栏。普通复位完成视频后可以返回工作；睡眠提醒没有正常返回按钮，只能长按 `Esc` 紧急退出，第一次退出后 1 分钟再次提醒。
