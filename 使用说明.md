# 身体复位提醒器

这是新版免安装桌面版。复制整个 `便携版` 文件夹到别人的电脑，双击里面的 `身体复位提醒器.exe` 就能使用。

## 便携版位置

当前可用程序在：

```text
便携版/身体复位提醒器.exe
```

`便携版` 里会保留：

```text
身体复位提醒器.exe
videos/
data/
```

用户只需要把复位视频放进 `videos` 文件夹，或者在里面建立按星期使用的子文件夹。`data` 是程序自己保存设置、播放记录和统计数据的位置，不需要手动编辑。

## 使用流程

1. 双击 `身体复位提醒器.exe`。
2. 把复位视频放进同级 `videos` 文件夹。
3. 回到主窗口，确认专注时长。
4. 点击“开始专注”进入倒计时。
5. 中途可以点击“暂停”或“重置”。
6. 倒计时结束后，会弹出复位窗口并播放一个视频。
7. 视频播放时可以按方向键 `↑` / `↓` 调节音量。
8. 视频播放完成前不能点击完成。
9. 视频结束后，点击“完成，回到工作”，程序会自动开始下一轮专注倒计时。

## 视频异常等待

“视频异常等待”只在视频打不开、播放失败等异常情况使用。比如设置为 5 分钟，意思是视频无法播放时等待 5 分钟后可以返回工作。

正常视频不受这个设置影响，仍然必须播放完成后才能点击完成。

## 今日节奏

主界面会显示今天的工作与运动休息节奏：

- 今日工作总时长
- 运动休息次数
- 复位总时长

这些数据由程序自动记录，不需要写配置。

## 身体账本

主界面会显示“身体账本”，可以切换：

- 本周数据表
- 本月数据表

数据表按天记录：

- 工作时长
- 复位次数
- 复位总时长
- 完成率

程序还会在 `data/daily-stats.csv` 自动生成一张日统计表，后续做每周/月奖励、惩罚或积分规则时，可以直接使用这份数据。

## 视频规则

支持这些格式：

- `.mp4`
- `.mov`
- `.wmv`
- `.avi`
- `.mkv`
- `.m4v`

如果有多个视频，程序会自动轮流播放，尽量避免连续两次播放同一个视频。

空文件会被自动忽略。如果某个视频大小是 0 字节，需要重新下载或重新复制。

可以在 `videos` 里建立按星期使用的子文件夹：

```text
videos/
  周一到周五/
  周末/
```

也可以更细：

```text
videos/
  周一/
  周二/
  周三/
  周四/
  周五/
  周六/
  周日/
```

选择顺序是：当天专属文件夹优先，其次是“周一到周五”或“周末”，都没有时使用 `videos` 根目录里的视频。

## 可视化设置

点击右上角设置按钮，可以调整：

- 专注时长
- 视频异常等待
- 视频文件夹
- 严格模式
- 最小化到托盘
- 开机启动
- 紧急退出秒数

开启“开机启动”后，程序随系统启动时会自动开始倒计时。

## 安全出口

复位窗口会尽量全屏置顶，并覆盖 Windows 任务栏。遇到误触发、电话、系统异常等情况，可以长按 `Esc` 紧急退出；这个提示不会显示在复位画面里。
